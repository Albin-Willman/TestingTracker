<%= simple_form_for(@test_suite) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.association :users, collection: @users.map { |user| [user.email, user.id] }, input_html: { multiple: true, class: 'js-chosen' } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
