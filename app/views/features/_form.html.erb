<%= simple_form_for([@test_suite, @feature]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :description_markdown %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
